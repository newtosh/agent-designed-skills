Vendored via `git subtree` from
https://github.com/anthropics/skills (Apache-2.0), path
`skills/frontend-design/`, at commit
`34040c9c568585f6929bedeaad110ad08f079624`. History preserved — see
`git log skills/frontend-design/`.

Upstream ships `LICENSE.txt` inside this directory. That file is part of
the subtree. This `ATTRIBUTION.md` is hand-maintained.

Update: `./vendor-update.sh frontend-design` (or `--all`). Driven by vendor.json.
