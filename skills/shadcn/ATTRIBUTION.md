Vendored via `git subtree` from
https://github.com/shadcn-ui/ui (MIT), path
`skills/shadcn/`, at commit
`98a1fe67b439324ddc857f47fbdce056600a4329`. History preserved — see
`git log skills/shadcn/`.

Upstream's license file is `LICENSE.md` at the repository root. The copy
in this directory is hand-maintained as `LICENSE`.

Update: `./vendor-update.sh shadcn` (or `--all`). Driven by vendor.json.
