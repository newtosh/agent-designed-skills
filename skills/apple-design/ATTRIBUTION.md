Vendored via `git subtree` from
https://github.com/emilkowalski/skills (MIT), path
`skills/apple-design/`, at commit
`85e8e2363b713506e1d5b6e07a0eb2da66be1bc3`. History preserved — see
`git log skills/apple-design/`.

Update: `./vendor-update.sh apple-design` (or `--all`). Driven by vendor.json.
