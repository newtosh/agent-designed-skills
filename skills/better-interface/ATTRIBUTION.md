Vendored via `git subtree` from
https://github.com/jakubkrehel/skills (MIT), path
`skills/better-interface/`, at commit
`267330e1adfc66a718fb65fa6918c1f06d0a689e`. History preserved — see
`git log skills/better-interface/`.

Update: `./vendor-update.sh better-interface` (or `--all`). Driven by vendor.json.
