Vendored via `git subtree` from
https://github.com/wshobson/agents (MIT), path
`plugins/ui-design/skills/interaction-design/`, at commit
`4236bb91f8395b0435f1d8b8baf9e8e4c69a8620`. History preserved — see
`git log skills/interaction-design/`.

Update: `./vendor-update.sh interaction-design` (or `--all`). Driven by vendor.json.
